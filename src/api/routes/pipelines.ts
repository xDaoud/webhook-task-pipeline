import { Router } from 'express';

const router = Router();

// POST   /pipelines
// GET    /pipelines
// GET    /pipelines/:id
// PATCH  /pipelines/:id
// DELETE /pipelines/:id

export default router;